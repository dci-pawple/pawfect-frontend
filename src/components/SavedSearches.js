import React from 'react'

const SavedSearches = () => {
    return (
        <div>
            <p>You have 0 saved searches set up for new pets.
                Saved searches watch Petfinder for you, so you can get a first look at any newly added pets.
            </p>
            <p>To set one up, use the 'SAVE SEARCH' button that appears above your search results.</p>
        </div>
    )
}

export default SavedSearches
